import { NavigateToDirective } from './navigate-to.directive';

describe('NavigateToDirective', () => {
  it('should create an instance', () => {
    const directive = new NavigateToDirective();
    expect(directive).toBeTruthy();
  });
});
